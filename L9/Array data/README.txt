+TASk
+Using Visual Studio, create a project using the ConsoleApplication template. 
+Required: Create an array of size N elements, fill it with arbitrary integer values ​​(the size of the array is set by the user). 
+Display: the largest array value, the smallest array value, the total sum of all elements, the arithmetic mean of all elements, display all odd values.

+plan
+create array
+to find Min, Max, sum, 
+odd elements