 + Using Visual Studio, create a project using the Console Application template.
 + Create an anonymous method that takes an array of delegates as an argument and 
 + returns the arithmetic mean of the return values ​​of the methods reported with the delegates in the array. 
 + Methods reported on delegates from an array return a random int value.