Task
-Using Visual Studio, create a project using the Console Application template.
-Required: Create a class named Rectangle.
-In the body of the class, create two fields describing the lengths of the sides double side1, side2.
-Create a custom constructor Rectangle (double side1, double side2), in the body of which
-the side1 and side2 fields are initialized with the argument values.
-Create two methods that calculate the area of ​​a rectangle - double AreaCalculator () and perimeter
-rectangle - double PerimeterCalculator ().
-Create two properties double Area and double Perimeter with one get accessor.
-Write a program that takes from the user the lengths of two sides of a rectangle and prints
-perimeter and area on the screen.

plan
+ создать конструктор Rectangle(double side 1, side 2)
+ создать метод double AreaCalculator()
+ Создать метод Double PerimeterCalculator()
+ создать два свойства Double Area и Double Perimetr С одним методом доступа get 
+ Сделать вывод на экран площадь и периметр
