+ Task
  + Using Visual Studio, create a project using the Console Application template.
  + Create an enumeration that will contain employee titles as constant names.
  + Assign each constant a value that specifies the number of hours an employee should work in a month.
  + Create an Accauntant class with the bool AskForBonus (Post worker, int hours) method, 
  + reflecting whether or not the employee should be given a bonus. 
  + If an employee has worked more than the prescribed hours per month, then he is entitled to a bonus.