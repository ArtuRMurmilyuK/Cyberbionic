 - Task
   - Create class Invoice.
   - In the body of the class, create three fields int account, string customer, string provider which should
   - be initialized once (when creating an instance of this class) without the possibility of them
   - further changes.
   - In the body of the class, create two private fields string article, int quantity
   - Create a method for calculating the cost of an order with and without VAT.
   - Write a program that displays the amount of payment for the ordered goods with or without VAT.